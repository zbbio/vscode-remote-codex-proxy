# Security Policy

## Reporting a vulnerability

请通过 GitHub 仓库的 **Security → Advisories → Report a vulnerability** 私下提交安全问题。

报告中可以包含：

- 受影响的脚本和版本；
- 使用合成路径与占位值复现的步骤；
- 预期行为、实际行为和影响；
- 建议修复或回滚方式。

请删除密码、令牌、Cookie、授权 URL、真实 SSH 配置、主机地址、账户名和未经打码的日志或截图。

普通功能问题可使用公开 Issue；可能导致凭据泄漏、错误覆盖程序、扩大监听范围或越过专用 SSH 隔离边界的问题应使用私下漏洞报告。

## Supported versions

安全修复应用于默认分支当前版本。版本化 release 建立后，本节应改为明确的支持矩阵。
